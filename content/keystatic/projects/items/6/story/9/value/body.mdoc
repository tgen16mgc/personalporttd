The competitor tool changed the most. Earlier versions could return loose links or a block of AI-written notes. The current flow records what was collected, what matched the requested advertiser, what has usable media, what a reviewer accepted, and what was discarded. The brief comes from the accepted set. If an AI provider is unavailable, local rules produce a clearly labeled fallback instead of pretending a model ran.
