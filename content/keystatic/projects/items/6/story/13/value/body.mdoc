The code includes audited write paths for connected Meta and Google accounts. Live use still depends on account permissions and production credentials. I capped a single budget move at 20%, added holds for learning-phase actions, and kept TikTok budget changes as recommendations until an approved owned-account connector exists. The interface states those limits directly.
